= form_for [@user, @wishlist, @wish] do |f|
  .fields
    = f.label :name
    = f.text_field :name

    = f.label :description
    = f.text_field :description

    = f.label :link
    = f.text_field :link

  = f.submit
